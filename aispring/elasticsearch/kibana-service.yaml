apiVersion: v1
kind: Service
metadata:
  name: kibana
  namespace: aispring
  labels:
    app: kibana
spec:
  ports:
    - name: http
      port: 9200
      nodePort: 9200
      targetPort: http
  type: NodePort
  selector:
    app: kibana
